import 'package:flutter/cupertino.dart';

class AppBoxShadows {
  static final regularXSmall = BoxShadow(
      color: const Color(0xFF0A0D14).withValues(alpha: 0.03),
      blurRadius: 2,
      offset: const Offset(0, 1)
  );
}